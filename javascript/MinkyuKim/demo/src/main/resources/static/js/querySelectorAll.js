function showDetail () {
    document.querySelector('#desc').style.display = "block";
    document.querySelector('#open').style.display = "none";
}

function hideDetail () {
    //diplay 옵션에 none이 들어가면 눈에보이는 것들이 사라짐(데이터가 아닌 시각적인 부분에서만)
    // block은 세로로배치
    document.querySelector('#desc').style.display = "none";
    document.querySelector('#open').style.display = "block";
}
// 1) id vueapp의 내용물(<p>)을 모두 가져온다.
// 2) 짝수번째만 visible 적용되도록 한다. 다시 누르면 홀수번째가 보이도록 한다.

// 뷰앱을 넣었다
var pList = [];
var query = document.querySelectorAll('#vueapp');

Btn.addEventListener("click", color);


pList.push(query);


    var item = name + ", " + age + ", " + major;
    

document.querySelectorAll('#vueapp').


var addBtn = document.querySelector("#add");

if()
